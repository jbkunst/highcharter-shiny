This app shows the minimal pattern for using Highcharter inside a Shiny application.

- `highchartOutput()` creates the chart container in the UI.
- `renderHighchart()` builds the chart on the server.
- The example combines a built-in Highcharts demo with an `iris` scatter plot.
