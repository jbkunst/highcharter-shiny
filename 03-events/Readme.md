Highcharter event helpers can expose chart interactions as Shiny input values.

This app compares point and series events for:

- `click`;
- `mouseOver`.

For an output named `hcpkmn`, the corresponding values are available through inputs such as `input$hcpkmn_click` and `input$hcpkmn_mouseOver`.
