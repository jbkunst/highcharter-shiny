This compact `bslib` example shows how point events from a Highcharter chart become reactive Shiny inputs.

- Clicking a point displays its coordinates in a card.
- Moving the pointer over a point displays the current hover coordinates.
- The input names are derived from the chart output ID and the event type.
